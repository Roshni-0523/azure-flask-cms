# azure-flask-cms
A cloud-based Content Management System (CMS) built using Flask and deployed on Microsoft Azure. The application uses Azure SQL Database for storing article data and Azure Blob Storage for storing uploaded images. 
